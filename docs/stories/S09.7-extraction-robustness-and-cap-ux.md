# S09.7 — Robustesse extraction MCP & UX des caps

> **Statut** : 🟢 phase 1 elicitation **figée 2026-04-26** — phase 2 dev
> peut démarrer.
> **Durée estimée phase 2** : ~5-6 h dev + 1.5 h validation live + 1 h
> doc/commit (cf. §"⏱ Estimations effort phase 2" en bas).
> **Parallélisable avec** : —
> **Prérequis** : S09.5 mergée (Payload Vault opérationnel) +
> [`S09.6`](./S09.6-mcp-workaround-and-credit-cache.md) mergée
> (workaround tools Pappers + cache persistant). Cette story était
> initialement S09.6 ; renommée S09.7 lors de l'insertion d'une story
> S09.6 dédiée au workaround côté crédits Pappers (cf. tracker
> [`README.md`](./README.md)).

---

## 🚦 Phase 1 — Décisions arrêtées (2026-04-26)

> Section ajoutée par l'agent d'élicitation au terme de la recherche
> web 2026 + dialogue utilisateur. Fige le scope phase 2. Toute
> divergence en phase 2 doit être justifiée par un commit dédié.

### ✅ Inputs utilisateur — résolus phase 1

- [x] **Scope "UX > anti-abus" confirmé** (réponse user 2026-04-26).
      Pas d'anti-abus dans S09.7. Story future quand le projet passera
      de "démo évaluateur" à "produit" (cf. §"Hors scope explicite").
- [x] **Walker JSON wildcard** : adopter `jsonpath-ng` 1.8.0 (réponse
      user 2026-04-26). 1 dep mature, CVE 2025-56005 patchée
      24/02/2026, couvre `$.arr[*].field`, `$..foo`, `$.*.foo`,
      slices. Le walker custom existant (livré S09.5 M1) garde la
      désambiguïsation par contexte pour les paths simples ; les paths
      contenant `[*]` / `..` / `*` sont délégués à `jsonpath-ng`. Cf.
      §"🏗 Architecture phase 1" axe 1.
- [x] **Token budget cible 80K → 200K** (réponse user 2026-04-26).
      S'aligne sur la context window Sonnet 4.6 (200K). Sans prompt
      caching, le coût Anthropic exploserait — c'est pourquoi le bump
      est **couplé** à l'activation du prompt caching (Axe 3 C5),
      indissociable.

### Axes retenus pour phase 2 (vue d'ensemble)

| Axe | Décision | Effort | Priorité |
|---|---|---|---|
| **1 A1** | ✅ déjà livré S09.5 M1 — désambiguïsation walker dict-vs-list | 0 (fait) | — |
| **1 A2** | ✅ retenu — wildcard `[*]` via `jsonpath-ng` 1.8.0 (1 dep) | ~1.5 h | haute |
| **1 A4** | ✅ retenu (light) — enrichir messages d'erreur walker avec liste clés disponibles | ~30 min | basse |
| **2 B1** | ✅ retenu — annotation `<dict_key:str>` pour clés numériques dans skeleton | ~30 min | moyenne |
| **2 B3** | ✅ retenu — sample d'un item type dans arrays (au lieu de `<dict>[N items]`) | ~45 min | haute |
| **3 C1** | ✅ retenu — bump `MAX_LOCAL_LOOKUPS_PER_TURN` 5 → 10 | 5 min | basse |
| **3 C2** | ✅ retenu — bump `MAX_TOKENS_PER_SESSION` 80K → **200K** | 5 min | haute (couplée C5) |
| **3 C3** | ❌ écarté — caps adaptatifs Sonnet vs Haiku (sur-ingénierie pour le gain) | — | — |
| **3 C4** | ✅ retenu — event `cap_continuation_proposed` non-bloquant en UI Chainlit | ~1.5 h | haute |
| **3 C5** | ✅ retenu — Anthropic prompt caching `cache_control` sur tools + system + messages[-1] | ~1.5 h | haute (couplée C2) |
| **4bis** | ✅ retenu — déplacer "Carte des tools" de `prompts.py` vers `description` MCP via `DESCRIPTION_OVERRIDES` dans `to_anthropic_schema` | ~30 min | haute (cohérence philosophie) |
| **4 D1** | ❌ écarté — ajouter `mandats|filiales` à COMPLEX_PATTERNS | — | — |
| **4 D2** | ❌ écarté — heuristique escalade auto sur taille payload | — | — |
| **5 E1** | ✅ retenu (intégré à 2 B3) — sample paths visibles dans le skeleton | inclus B3 | — |
| **5 E2** | ❌ écarté — tool `payload_keys` (redondant avec wildcards de A2) | — | — |
| **5 E3** | ✅ retenu — hint d'introspection itérative dans `_inspect_hint` | inclus B3 | — |
| **Méta-CE** | ❌ écarté — `clear_tool_uses_20250919` (beta toujours, server-side compaction GA est l'alternative préférée 2026 — la noter en next-step S10+, pas dans S09.7) | — | — |

**Justifications "écarté"** :

- **C3 (caps adaptatifs)** : ajoute de la complexité contre un gain
  marginal — bumper le cap à 200K + prompt caching couvre déjà les
  worst cases observés (G3) sans introduire un branchement
  Haiku-vs-Sonnet dans `caps.py`. La philosophie "agent adaptable"
  préfère un cap unique que des règles conditionnelles.
- **D1 (routing keywords)** : taggerait "mandats" / "filiales" comme
  complexes → escalade Sonnet systématique. Contraire à la
  philosophie "agent autonome qui se débrouille" — mieux vaut donner
  à Haiku les bons outils (wildcard `[*]` + sample dans skeleton)
  que de lui retirer le rôle. Si Haiku continue à plafonner après
  S09.7, on rouvrira en S09.8.
- **D2 (heuristique escalade auto sur taille)** : trop de couches
  implicites, agent moins prévisible — préférer compter sur
  l'auto-escalation Haiku (`escalate_to_sonnet` tool) que sur des
  règles backend cachées.
- **E2 (tool `payload_keys`)** : avec `jsonpath-ng` qui supporte
  `$.foo[*].field`, l'agent peut extraire en 1 call ce qu'il
  faisait en N (un par index). Plus de tool dédié à l'exploration =
  moins de surface pour le LLM, plus de simplicité.
- **`clear_tool_uses_20250919`** : ajoute une couche beta
  (header `context-management-2025-06-27`) avec invalidation cache
  partielle. Server-side compaction (GA en 2026) est la "primary
  strategy" recommandée par Anthropic — on s'y tournera plus tard si
  le besoin se confirme. Hors scope S09.7.

---

## 📍 Contexte

Le **dogfooding S09.5** (golden pack G1-G5 joué en live le 2026-04-25,
cf. [`traces/S095_iterations.md`](../../traces/S095_iterations.md)) a
validé que l'offload générique du Payload Vault élimine la troncature
naïve (objectif principal de S09.5 : ✅). Mais il a aussi **mis en
lumière 3 axes de fragilité** qui dégradent l'expérience utilisateur,
sans pour autant remettre en cause l'architecture vault retenue :

1. **Robustesse de l'extraction sur schémas JSON inattendus** — le
   walker `payload_inspect` interprète mal certains paths (clés
   numériques traitées comme indices), et le skeleton ne donne pas
   assez de signal pour que l'agent choisisse le bon path du premier
   coup.
2. **Caps qui terminent brutalement la conversation** — quand un cap
   firefires (`cap_token_budget`, `cap_local_lookups_per_turn`), le
   tour s'arrête sans option utilisateur. La conversation devient un
   "dead end" alors que le contexte (notamment le vault) pourrait
   parfaitement être réutilisé sur un tour suivant.
3. **Coût tokens élevé sur conversations multi-rounds** — sans
   `prompt caching` activé, chaque round Claude re-tokenize l'intégralité
   du système + des tools + de l'historique cumulé. Cause directe du
   `cap_token_budget=80K` qui firefires souvent en fin de tour
   (déjà listé en next-step #1 du `README.md`).

Ces 3 axes se renforcent mutuellement : un cap qui firefires sur un
schéma incongru transforme une question résolvable en frustration
utilisateur. **L'objectif de S09.7 est de transformer ces frictions en
expérience fluide**, sans sacrifier la robustesse acquise par S09.5
ni introduire des sur-coûts non maîtrisés.

### 🎯 Objectif final (philosophie agent — confirmation utilisateur 2026-04-26)

> *« En fait je ne veux pas que notre agent soit stéréotypé, robotique,
> "bête", on peut pas hardcoder des consignes ou comportement, parce que
> sinon quand Fabien va tester il va poser des questions, des phrases non
> prévues, et notre agent sera largué. En fait dans les faits je veux que
> notre agent (et Haiku et Sonnet sont suffisamment bons pour ça) ait une
> liste de tools très clairement définis parmi lesquels il peut choisir, un
> utilisateur pose une question il se débrouille, est inventif, autonome
> pour répondre à son utilisateur, si un tool marche pas il essaie un autre,
> il navigue intelligemment dans le payload si énorme, bref il est
> adaptable. »*

Cette note **prime sur tous les autres axes** : tout fix proposé doit
préserver (ou améliorer) cette adaptabilité plutôt que pousser l'agent
vers un script. Concrètement :

- ✅ **Bons leviers** : meilleur outillage du vault (wildcard `[*]`,
  skeleton avec sample d'item type), tool helper `payload_keys` pour
  exploration adaptive, cap soft + event de continuation, prompt
  caching Anthropic, enrichissement des `description` de tools
  (uniquement clarifications de schéma input/output, jamais de
  logique métier).
- ❌ **À éviter** : verbatim de phrases à dire, règles "if X then
  call Y", prescriptions de comportement injectées dans `prompts.py`
  ou dans un `tool_result` synthétique. Ces patterns transforment
  l'agent en checklist et le rendent fragile face à toute question
  "non prévue" par le concepteur — exactement ce que Fabien testera.

### Hors scope explicite (décision utilisateur 2026-04-25)

Cette story **n'aborde pas** les protections anti-abus (`MAX_$_PER_SESSION`,
rate-limit per-IP, idempotence cumulative, audit trail). À ce stade,
le projet privilégie la **qualité d'expérience utilisateur** sur les
mécanismes de protection commerciale (qui auront leur propre story
quand le déploiement passera de "démo évaluateur" à "produit"). La
phase 1 d'élicitation **ne doit pas** s'égarer sur ces sujets.

Sources de vérité :

- [`traces/S095_iterations.md`](../../traces/S095_iterations.md) —
  journal des observations live G1-G5 (Step 1/2/3) avec traces JSONL
  référencées.
- [`docs/stories/S09.5-mcp-payload-handling.md`](./S09.5-mcp-payload-handling.md)
  — story parente qui a livré le Payload Vault.
- [`src/genial_agent/payload_vault.py`](../../src/genial_agent/payload_vault.py)
  — module à étendre (walker, skeleton, hints).
- [`src/genial_agent/agent.py`](../../src/genial_agent/agent.py)
  — pipeline tool-use où le cap UX se joue.
- [`src/genial_agent/guardrails/caps.py`](../../src/genial_agent/guardrails/caps.py)
  — single source of truth des caps.
- [`src/genial_agent/guardrails/pipeline.py`](../../src/genial_agent/guardrails/pipeline.py)
  — endroit où les events `capped` sont émis.
- [`src/genial_agent/routing.py`](../../src/genial_agent/routing.py)
  — pré-routeur keyword (Haiku/Sonnet).
- [`README.md`](../../README.md) §"Next steps" #1 — prompt caching
  Anthropic.

---

## 🔒 Prérequis

- [x] Story S09.5 mergée et stable (vault + tools locaux opérationnels).
- [x] Pack adversarial S09 reste **10/10** (validé après S09.5).
- [x] Pack golden G1-G5 reste **5/5** (validé après S09.5, avec G2 à
      cible ajustée 3 SIRENs et G4 sur assertion lâche).
- [ ] **Pas de story S10 démarrée tant que S09.7 n'est pas mergée** —
      éviter les conflits sur `agent.py` / `routing.py` / `pipeline.py`.

## 🔑 Inputs utilisateur — résolus phase 1

- [x] **Scope "UX > anti-abus" confirmé** (réponse user 2026-04-26) :
      la priorisation tient. Anti-abus reste hors scope.
- [x] **Arbitrage approches** : axes 1 A2, 2 B1+B3, 3 C1+C2+C4+C5,
      4bis, 5 E1+E3 retenus. Axes 3 C3, 4 D1, 4 D2, 5 E2 écartés.
      Détail §"🚦 Phase 1 — Décisions arrêtées".
- [x] **Token budget cible** : `MAX_TOKENS_PER_SESSION` **80K → 200K**
      (s'aligne sur context window Sonnet 4.6). Couplé à activation
      prompt caching (cache read = 0.1× input price, le coût
      Anthropic reste maîtrisé même au cap 200K).

---

## 🎯 Scope

### Dans le scope

1. **Fix walker `_walk` de `payload_vault.py`** : le segment de path
   numérique pur (`"2023"`, `"775670417"`) doit pouvoir être interprété
   comme **clé string de dict** quand le nœud courant est un dict, pas
   forcément comme **index d'array**. La désambiguïsation est faite
   par le contexte (type de `cur`).
2. **Amélioration du skeleton et des hints** dans `build_index` :
   donner à l'agent assez de signal pour choisir le bon path du premier
   coup, sans hardcoder de logique métier (cf. contrainte S09.5
   "pas de hardcoding").
3. **Recalibrage des caps avec UX différenciée** :
   - Caps "compute pur" (lookups vault) : à relâcher.
   - Caps "argent" (Pappers credits, Anthropic tokens) : à conserver
     ou à bumper en concertation avec activation du prompt caching.
   - Caps qui firefires : transformer en **event UX continuable**
     plutôt que terminaison brutale.
4. **Activation Anthropic prompt caching** sur `system` + `tools`
   schema + messages N-1 (déjà listé en next-step #1 README, à
   intégrer dans cette story par cohérence : sans prompt caching, le
   relâchement du cap tokens deviendrait coûteux).
5. **Pack golden G1-G5 doit passer avec assertions resserrées** :
   - G2 : remonter la cible de 3 SIRENs vers 7-10 (story S09.5
     §"Métrique de succès objective" disait 10).
   - G4 : remonter l'assertion de "mention 'résultat net' + '2023'"
     vers "valeur exacte du résultat net 2023 citée".
6. **Pack adversarial S09 : 10/10 maintenu** (non-régression dure).
7. **Documentation** : update `traces/S095_iterations.md` (section
   "Après S09.7"), `README.md` (next-step #1 marqué livré),
   `docs/stories/README.md`.

### Hors scope (explicite)

- **Anti-abus utilisateur** : `MAX_$_PER_SESSION`, rate-limit per-IP,
  audit trail, idempotence cumulative — décision produit 2026-04-25
  (cf. §"Hors scope explicite" du contexte). Story future quand le
  passage démo → produit sera décidé.
- **Wrapper MCP-specific par tool** (déjà refusé en S09.5).
- **Re-design fondamental de l'agent** (multi-agent, framework custom).
- **Brief vocal ElevenLabs** (S10, séparé).
- **Bedrock EU / Vertex EU** (next-step #3 README, story dédiée).

---

## 📊 Observations factuelles — golden pack G2-G5 (live 2026-04-25)

Reproductible : `uv run python scripts/run_golden_S095.py`. Traces
JSONL complètes dans `traces/S095_*.jsonl`. Synthèse des 4 cas :

### G2 — *"Quels sont les mandats de Bernard Arnault ?"*

| Métrique | Observation |
|---|---|
| Routing | Haiku (par défaut, "mandats" pas dans `COMPLEX_PATTERNS`) |
| Pappers calls | 2 (`recherche-dirigeants` × 2 args différents) |
| Payloads offloadés | 2 (77K + 168K chars) |
| Local lookups | 4 (1 search regex + 3 inspects, dont 1 raté `[*]`) |
| Cap firefired | `cap_token_budget=80K` à mi-tour |
| SIRENs cités dans la réponse | **7** (cible story 10) |
| Marker "tronqué" | absent ✅ |

**Cause racine du gap 7 vs 10** : Pappers retourne 39 homonymes
"Bernard Arnault" → l'agent doit (a) disambiguer le "vrai" Arnault,
(b) extraire ses entreprises de `resultats[i].entreprises`, (c) lister
exhaustivement. Étapes (a) et (b) consomment ~80K tokens cumulés avant
que (c) ne soit complète.

**Tentative ratée notable** : `payload_inspect(json_path="$.resultats[*]")`
→ `_error: "key '*' missing at '$.resultats[*]'"`. Le walker ne
supporte pas le wildcard JSONPath. L'agent corrige seul mais consomme
1 lookup.

### G3 — *"Compare la santé financière de Carrefour vs Casino sur 3 ans"*

| Métrique | Observation |
|---|---|
| Routing | **Sonnet** (keyword "compare") |
| Pappers calls | 4 (2 sirenisateur + 2 comptes-entreprise avec `annee=2021,2022,2023`) |
| Payloads offloadés | 2 (240K + 161K chars) |
| Local lookups | 2 (searches multi-pattern très efficaces) |
| Cap firefired | `cap_token_budget=80K` en fin de stream (UX inchangée) |
| Réponse | Tableau 5 × 3 ans, **71 chiffres datés**, sourçage explicite |

**Verdict** : ✅ — le cas le plus complexe passe sans accroc, démonstration
de l'efficacité de `payload_search` regex multi-pattern sur les payloads
financiers. Note : 1 retry nécessaire au pytest (transient
`RateLimitError` Anthropic après ~10 min de tests cumulés).

### G4 — *"Quel est le résultat net de LVMH 2023 ?"*

| Métrique | Observation |
|---|---|
| Routing | Haiku |
| Pappers calls | 2 (sirenisateur + comptes-entreprise(annee=2023)) |
| Payloads offloadés | 1 (55K chars) |
| Local lookups | **5/5 (cap atteint)** sans extraction de la valeur |
| Tentatives ratées | `$.2023[0]` → `_error: "not a list at '2023'"` ; `$.2023` idem ; 2 searches regex `resultat.*net` → 0 matches |
| Inspect partiel | `$.comptes[0]` → 598 chars (compte 2024) |
| Réponse finale | 433 chars, **pas de valeur chiffrée**, mention "résultat net" + "2023" présente ✅ (assertion lâche) |

**Cause racine** : le payload Pappers a deux clés top-level `"2023"`
(string-key dict) et `"comptes"` (array). Le `_skeleton` rend
`"2023": ["<dict>[2 items]"]` qui ressemble à une liste, et le walker
traite tout segment numérique pur comme un index d'array — donc
`$.2023[0]` est interprété comme `node[2023][0]` au lieu de
`node["2023"][0]`. Bug réel du module S09.5 identifié post-merge.

### G5 — *"Liste les filiales de LVMH"*

| Métrique | Observation |
|---|---|
| Routing | Haiku |
| Pappers calls | 2 (sirenisateur + cartographie-entreprise) |
| Payloads offloadés | 1 (26K chars) |
| Local lookups | **1** inspect bien ciblé (`$.entreprises`) → 12K chars retournés |
| Cap firefired | `cap_token_budget=80K` en fin de stream (UX inchangée) |
| Réponse | **22 SIRENs distincts** structurés en 5 catégories |

**Verdict** : ✅ — cas idéal montrant qu'1 lookup suffit pour
extraire 100+ entités d'un payload de 26K chars. Pattern à généraliser.

### Tableau récapitulatif

| Test | Routing | Lookups | Cap atteint | Réponse OK ? | Limite identifiée |
|---|---|---:|---|---|---|
| G1 (Carrefour CA) | Haiku | 5 | tokens | ✅ valeur exacte | tokens limite, pas bloquant |
| G2 (Arnault mandats) | Haiku | 4 | tokens | ⚠️ 7/10 SIRENs | routing + tokens |
| G3 (Carrefour vs Casino) | Sonnet | 2 | tokens (tardif) | ✅ tableau complet | rien (cap UX OK) |
| G4 (LVMH résultat 2023) | Haiku | **5/5** | lookups + tokens | ⚠️ pas de valeur | walker bug + skeleton |
| G5 (LVMH filiales) | Haiku | 1 | tokens (tardif) | ✅ 22 SIRENs | rien (cap UX OK) |

---

## 🧭 Pistes d'amélioration — arbitrées phase 1 (2026-04-26)

> **Pour le dev agent S09.7 phase 2** : la phase 1 a tranché les
> pistes ci-dessous. Le tableau récapitulatif §"🚦 Phase 1 — Décisions
> arrêtées" donne la liste exécutable. Les détails ci-après restent
> utiles pour comprendre **le raisonnement** derrière chaque choix —
> ils ne sont plus à arbitrer.

### Axe 1 — Robustesse du walker JSON (`_walk` de `payload_vault.py`)

**Symptôme** : G4 — `$.2023[0]` rejeté parce que `"2023"` est traité
comme index alors que c'est une clé string. Aussi : `$.resultats[*]`
non supporté en G2.

**Pistes brutes (à arbitrer)** :

- **A1 — ✅ LIVRÉ EN S09.5 POST-REVIEW (2026-04-25)** Désambiguïsation
  par contexte : si `cur` est un dict → segment toujours interprété
  comme clé string ; si `cur` est une list → segment digit interprété
  comme index. Effort réel : ~5 lignes de code + 6 tests unit. Cf.
  [`traces/S095_iterations.md`](../../traces/S095_iterations.md)
  §"Step 4". **Reste à faire** : valider en live G4 dès retour des
  crédits Pappers et resserrer l'assertion `_has_resultat_net_2023`
  vers la valeur exacte du résultat net.
- **A2** Support du wildcard `[*]` (extraction de toutes les valeurs
  d'un array en une opération). Permet à l'agent de récupérer en 1
  call ce qui en demanderait N aujourd'hui (ex : tous les
  `resultats[i].entreprises` en G2). Trade-off : taille de retour
  potentiellement énorme → cap `max_chars` doit gérer.
- **A3** Support des paths bracketés style `$["2023"][0]` pour forcer
  l'interprétation clé-string même sans contexte. **Devient moins
  prioritaire après A1 livré** (la désambiguïsation par contexte couvre
  le cas usuel sans nécessiter une syntaxe alternative).
- **A4** Acceptation tolérante : si A1 échoue (clé string introuvable
  dans dict), tenter A2 (wildcard) ou retourner suggestion `_error`
  avec liste des clés disponibles. Le fix A1 améliore déjà les
  messages d'erreur (taille de la list, type du nœud) — A4 pourrait
  encore enrichir avec la liste des clés disponibles.

### Axe 2 — Skeleton plus informatif (`build_index`)

**Symptôme** : G4 — l'agent ne peut pas distinguer dict-avec-clé-numérique
d'un array dans le skeleton. G2 — l'agent ne sait pas que `resultats[0]`
peut ne pas être le "bon" Bernard Arnault parmi les 39.

**Pistes brutes (à arbitrer)** :

- **B1** Annotation explicite des dicts à clés numériques :
  `"_skeleton": {"2023": "<dict_key:str>"}` ou wrapping
  `"(str_key)2023": ...`.
- **B2** Profondeur adaptative selon densité : si le nœud à profondeur
  3 est un dict simple (≤ 5 clés), descendre à profondeur 4 ; sinon
  rester à 3.
- **B3** Sample d'un élément type dans les arrays : au lieu de
  `[" <dict>[39 items]"]`, exposer `[{"siren": "<str:9>", "nom_entreprise":
  "<str:N>", ...}, "...37 more items"]` pour que l'agent connaisse les
  champs sans avoir à inspecter.
- **B4** Hint contextuel dans `_inspect_hint` : générer dynamiquement
  une suggestion de path en se basant sur le squelette ("Si tu cherches
  une valeur dans un array trié, essaie `$.<array>[-1]` pour le dernier
  élément").

### Axe 3 — Recalibrage des caps & UX de continuation

**Symptôme** : G2 et G4 — caps firefires brutalement, l'agent s'arrête
sans option claire pour l'utilisateur. G2 : `cap_token_budget`. G4 :
`cap_local_lookups_per_turn`.

**Pistes brutes (à arbitrer)** :

- **C1** Bump `MAX_LOCAL_LOOKUPS_PER_TURN = 5 → 10` ou plus. Compute
  pur, zéro coût €. Effort : 1 ligne + ajustement tests.
- **C2** Bump `MAX_TOKENS_PER_SESSION = 80K → 150K` (ou plus, vérifier
  contexte 200K Sonnet 4.6). À combiner avec C5 sinon coût explose.
- **C3** Caps adaptatifs selon complexité détectée (`pick_initial_tier`
  S04 déjà existant) : Sonnet → budgets supérieurs, Haiku → budgets
  serrés. Cohérent avec philosophie routing existante.
- **C4** Cap **soft** = event "budget_warning" à 80% du budget +
  event "continuation_proposed" quand cap atteint, au lieu de
  termination brutale. UI Chainlit affiche `[Continuer] [Synthèse]` ;
  user clique → next turn avec vault préservé. Préserve l'UX flow
  conversationnel.
- **C5** Activation Anthropic prompt caching (`cache_control` sur
  system + tools schema + messages N-1). Réduit input tokens facturés
  ÷ 10 sur rounds 2+, TTFT ÷ 5-10. **Déjà listé en next-step #1
  README**, à intégrer ici par cohérence (sinon C2 explose le coût).
  Effort estimé : ~1h.

### Axe 4 — Routing keywords pour requêtes énumératives

**Symptôme** : G2 — "mandats de X" routé Haiku, qui sature à 7 résultats.
Sonnet aurait probablement listé les 10+ attendus en moins de tokens
grâce à sa meilleure synthèse.

**Pistes brutes (à arbitrer)** :

- **D1** Ajouter `mandats|filiales|liste\s+les?|énumère|tous\s+les` à
  `COMPLEX_PATTERNS` du pré-routeur. Trade-off : peut sur-router et
  augmenter le coût Sonnet (vérifier impact sur U1 / pack adversarial).
- **D2** Heuristique "queries énumératives détectées par length du
  retour Pappers attendu" — si l'agent vient de recevoir un payload
  > 50K avec un array > 20 items, escalader auto vers Sonnet pour
  l'extraction. Plus subtil que D1 mais plus coûteux à coder.
- **D3** Garder Haiku par défaut + améliorer son system prompt avec
  un exemple concret d'extraction multi-entités via vault. Approche
  prompt-engineering pure.

### Axe 4bis — Déplacer la "Carte des tools" du system prompt vers les `description` MCP (additif S09.6 → S09.7)

**Symptôme** : la "Carte des tools Pappers" actuellement présente dans
``src/genial_agent/prompts.py`` (~30 lignes, livrée S09.6) utilise une
structure prescriptive ``CA année courante → recherche-entreprises avec
return_fields=[...]``. Elle aide observable sur G1 (l'agent applique
directement) mais c'est une **règle injectée dans le system prompt**,
contraire à l'objectif d'adaptabilité (cf. §"Objectif final").

**Approche retenue (audit S09.6 post-implémentation, 2026-04-26)** :
les informations de la carte sont **utiles** mais doivent vivre dans
les `description` des tools eux-mêmes (côté MCP schema), pas dans le
system prompt. C'est le pattern Anthropic standard : un tool est
auto-documenté par son schéma, l'agent le lit en contexte de tool
selection sans qu'on injecte de règles transversales.

**Plan** :

1. Étendre ``mcp_pappers.py:to_anthropic_schema`` avec un mapping
   ``DESCRIPTION_OVERRIDES: dict[str, str]`` qui préfixe/postfixe la
   description Pappers originale par les clarifications de schéma
   utiles (input vs return_fields, coût en crédits, type de payload
   attendu).
2. Borne stricte : **uniquement des clarifications de schéma** que
   Pappers aurait dû documenter (e.g. *"`annee_finances` est un nom
   de champ, à passer dans `return_fields`, pas un argument top-level"*).
   Pas de logique métier (pas de SIREN figés, pas de "pour LVMH fais X").
3. Supprimer en parallèle le bloc "## Carte des tools Pappers" de
   ``prompts.py`` (le system prompt redevient minimal et générique).
4. Vérifier observable : G1 doit continuer à passer (l'agent doit
   trouver la même info via le tool schema).

**Bénéfice** : un tool ajouté/retiré côté Pappers se documente
automatiquement, sans toucher au system prompt. Une question voisine
non prévue (ex : *"quel est le code NAF de X ?"*) bénéficie aussi des
descriptions enrichies sans qu'on ait à ajouter une nouvelle puce de
règle.

**Effort estimé** : ~30 min (mapping + suppression bloc prompt + run
G1 live pour confirmer non-régression). À grouper avec Axe 5 E1
(sample paths) qui touche aux mêmes signaux d'auto-documentation.

### Axe 5 — Field naming variability (cf. G4)

**Symptôme** : G4 — `payload_search(pattern="resultat.*net")` → 0 matches
parce que Pappers utilise `benefice_net` ou `resultat_de_lexercice`
selon les champs. L'agent ne devine pas les noms exacts.

**Pistes brutes (à arbitrer)** :

- **E1** Suggérer dans `_inspect_hint` un sample de paths tirés du
  skeleton pour que l'agent voie les vrais noms de champs. Cf. piste B4.
- **E2** Tool local supplémentaire `payload_keys(payload_id, json_path)`
  qui retourne uniquement les clés à un nœud donné, pour aider l'agent
  à explorer rapidement. Trade-off : 1 tool de plus à documenter dans
  le system prompt.
- **E3** Améliorer le hint pour pousser l'agent à inspecter d'abord
  un sample (`$.comptes[0]`) avant de lancer une regex globale —
  pattern d'introspection itérative.

### Méta-axe — Autres optimisations possibles

L'agent d'élicitation est libre de proposer d'autres pistes que celles
ci-dessus si la recherche web 2026 ou l'inspection du code en révèle.
Examples non-exhaustifs :

- **Streaming inspects** : permettre à `payload_inspect` de retourner
  progressivement (générateur), utile pour les très gros sous-arbres.
- **Compression du contexte historique** via `clear_tool_uses_20250919`
  (context editing beta Anthropic, cf. story S09.5 §"Approche E").
- **Persistance vault cross-turn** explicitement documentée (le vault
  est déjà par-session ConversationState, mais c'est implicite).

---

## 🏗 Architecture phase 1 — détaillée

> Cette section fige les snippets de référence pour la phase 2. Tout
> écart doit être justifié dans le commit dev. Sources web 2026
> consultées en bas de doc §"📚 Sources et références".

### Axe 1 A2 — `jsonpath-ng` 1.8.0 pour wildcards `[*]` / `..` / `*`

**Dépendance à ajouter** dans `pyproject.toml` :

```toml
dependencies = [
    # … existant …
    # S09.7 (Axe 1 A2) — wildcard JSONPath pour le payload_vault.
    # 1.8.0 corrige CVE-2025-56005 (pickle dans ply vendored).
    # API : jsonpath_ng.ext.parse(expr).find(data) → list[DatumInContext].
    # On utilise le module .ext (jsonpath-rw-ext) qui supporte filtres,
    # slices, recursive descent ; déjà inclus dans le wheel jsonpath-ng.
    "jsonpath-ng>=1.8.0,<2",
]
```

**Pattern d'intégration** dans `payload_vault.py:_walk` — délégation
conditionnelle, pas remplacement :

```python
# Tokens jsonpath-ng (wildcards / recursive descent / glob clé).
# Si le path utilisateur en contient un, on délègue à la lib mature.
# Sinon, on garde le walker custom (S09.5 M1) qui désambiguïse
# dict-vs-list par contexte (couvre `$.2023[0]` sans bracket syntax).
_JSONPATH_WILDCARD_TOKENS = ("[*]", "..", "[?")


def _walk(node: Any, path: str) -> Any:
    """Walk avec fallback wildcard via jsonpath-ng 1.8.0."""
    if not isinstance(path, str):
        return {"_error": f"json_path must be a string, got {type(path).__name__}"}

    if any(tok in path for tok in _JSONPATH_WILDCARD_TOKENS):
        return _walk_with_jsonpath_ng(node, path)

    # Walker simple existant (M1 livré S09.5) — désambiguïsation par
    # contexte pour les paths sans wildcard. Inchangé.
    return _walk_simple(node, path)


def _walk_with_jsonpath_ng(node: Any, path: str) -> Any:
    """Délègue à jsonpath-ng pour les paths avec wildcard.

    Retourne soit la valeur unique (1 match), soit la liste de valeurs
    (≥ 2 matches), soit ``{"_error": ...}`` si parse error / 0 match.
    """
    from jsonpath_ng.ext import parse  # lazy import : évite +50ms startup
    try:
        expr = parse(path)
    except Exception as exc:
        return {"_error": f"invalid jsonpath '{path}': {exc}"}
    matches = [m.value for m in expr.find(node)]
    if not matches:
        return {"_error": f"no match for '{path}' in payload"}
    if len(matches) == 1:
        return matches[0]
    return matches
```

**Cap retours wildcard** : un `$.resultats[*].entreprises` peut
retourner un array de 39 sous-listes (cas G2). La taille totale doit
respecter `LOOKUP_MAX_CHARS_HARD = 12_000` après `json.dumps` — borne
déjà implémentée dans `inspect()`. Le test ajoute :
`test_walk_wildcard_returns_array_of_matches` (avec assertion sur
`len(result)` ≥ 2).

**Mise à jour `description` du tool `payload_inspect`** dans
`agent.py:LOCAL_PAYLOAD_TOOLS` — clarifier que les wildcards
fonctionnent désormais (utile pour le LLM qui le tentait déjà en G2) :

```python
"description": (
    "Read a verbatim subtree from a previously offloaded MCP payload. "
    "Use the payload_id from the index. Path syntax: dotted with "
    "optional `$` prefix and `[i]` indices, e.g. '$.items[0].field'. "
    "Wildcards: '$.items[*].field' returns all matches as an array; "
    "'$..foo' recursive descent. A numeric segment is interpreted "
    "as a string-key when the current node is a dict (handy for "
    "date-keyed payloads like '$.2023[0]') and as an integer index "
    "when it is a list. Returns the JSON-encoded literal value."
),
```

### Axe 1 A4 — Enrichir messages d'erreur du walker

Quand `_walk` retourne `_error: "key 'X' missing at '...'"`, on
ajoute la liste des **clés disponibles** au nœud parent (jusqu'à 10) :

```python
return {
    "_error": f"key '{seg}' missing at '{path}'",
    "_available_keys": list(cur.keys())[:10],  # max 10
}
```

Le LLM peut ainsi corriger sans relancer un `payload_inspect` à
l'aveugle. Couvert par test
`test_walk_missing_key_lists_available`.

### Axe 2 B1 — Annotation clés numériques dans skeleton

Le skeleton actuel rend `{"2023": [...]}` comme
`{"2023": ["<dict>[2 items]"]}` — le LLM ne sait pas si "2023" est
une clé string ou un index. Annotation explicite quand la clé
ressemble à un nombre :

```python
def _skeleton(node: Any, depth: int) -> Any:
    if depth >= SKELETON_MAX_DEPTH:
        return _summarize(node)
    if isinstance(node, dict):
        items = list(node.items())[:SKELETON_MAX_KEYS_PER_LEVEL]
        out = {}
        for k, v in items:
            # Annotation S09.7 B1 : la clé est numérique pure mais
            # c'est bien une string-key (pas un index). On préfixe la
            # clé par un marqueur lisible pour le LLM.
            display_k = f"{k}↹" if isinstance(k, str) and k.lstrip("-").isdigit() else k
            out[display_k] = _skeleton(v, depth + 1)
        return out
    # … reste inchangé …
```

Le marqueur `↹` (U+21B9) est neutre (pas de balise XML, pas
d'instruction) et lisible — alternative à `<dict_key:str>` proposé
en piste B1 brute (jugée trop verbose). À documenter dans
`_inspect_hint`.

### Axe 2 B3 — Sample d'item type dans arrays

Au lieu de `[" <dict>[39 items]"]`, exposer un **vrai sample du
1er élément** + le compteur :

```python
def _skeleton(node: Any, depth: int) -> Any:
    # … début inchangé …
    if isinstance(node, list):
        if not node:
            return []
        first = node[0]
        if isinstance(first, dict):
            # Sample : premier item à profondeur+1 avec ses clés
            # (pas la valeur), tronqué à SKELETON_MAX_KEYS_PER_LEVEL.
            sample_keys = list(first.keys())[:SKELETON_MAX_KEYS_PER_LEVEL]
            sample = {k: _summarize(first[k]) for k in sample_keys}
            return [sample, f"…{len(node)-1} more dict items"] if len(node) > 1 else [sample]
        if isinstance(first, list):
            return [f"<nested list[{len(first)}]>", f"…{len(node)-1} more lists"]
        return [_summarize(first), f"…{len(node)-1} more {type(first).__name__} items"]
    return _summarize(node)
```

**Bénéfice G2 / G4** : l'agent voit que `recherche-dirigeants` array
contient `{"siren": "<str:9>", "nom_entreprise": "<str:N>",
"role": "<str:N>", ...}` → choisit `$.resultats[*].siren` du
premier coup au lieu de tâtonner. Idem G4 : visualise que
`comptes-entreprise.bilans[0]` contient `{"compte_resultat":
{...}, "annee_cloture": "<int>", ...}` et tape
`$.bilans[*].compte_resultat.benefice_net`.

**Cap budget** : le sample ajoute ~150-300 chars/array vs
`<dict>[N items]`. Sur un payload Carrefour (44K chars,
~12 arrays) → +2 K à l'index. `_shrink_index` ajusté pour
re-tronquer si dépassement de `INDEX_BUDGET_CHARS = 4_000`.

### Axe 3 C1 + C2 — Bumps caps

```python
# guardrails/caps.py
# S09.7 — bumped 5 → 10. Lookups locaux = compute pur (vault
# in-memory), pas de coût €. Cap dur conservé pour borner les
# prompts pathologiques où le LLM bouclerait.
MAX_LOCAL_LOOKUPS_PER_TURN = 10

# S09.7 — bumped 80_000 → 200_000. S'aligne sur la context
# window Sonnet 4.6. Couplé OBLIGATOIREMENT à l'activation du
# prompt caching (cf. agent.py) sinon le coût Anthropic explose
# sur U3 multi-round. Cache read = 0.1× input price → ROI dès
# le 2ème round. Cf. story §"🏗 Architecture phase 1" axe 3 C5.
MAX_TOKENS_PER_SESSION = 200_000
```

### Axe 3 C5 — Anthropic prompt caching `cache_control`

**Pré-requis Anthropic 2026** :
- Min cache : Haiku 4.5 = **4 096 tokens**, Sonnet 4.6 = 2 048.
- Max breakpoints : 4 par requête.
- TTL 5 min (default) ou 1h (`"ttl": "1h"`, 2× input price).
- Cache write = 1.25× input price, cache read = 0.1× input price.
- `cache_control` se pose sur le **dernier élément** du préfixe à
  cacher (dernier tool, dernier system block, dernier message
  block).

**Stratégie 3-couches retenue** dans `agent.py:run_turn` :

```python
# (1) Tools : on cache le préfixe Pappers + LOCAL_PAYLOAD_TOOLS
# qui sont stables session-life. ESCALATE_TOOL ajouté en queue
# côté routing, sans cache_control (changeable selon tier).
tools_schema = mcp_pappers.to_anthropic_schema(pappers_tools)
tools_schema = tools_schema + LOCAL_PAYLOAD_TOOLS
# Marquer le DERNIER tool de ce préfixe stable comme cacheable.
# extra_tools (escalate) ajouté APRÈS ce breakpoint = pas dans le
# cache, mais cache_control reste valide.
if tools_schema:
    tools_schema = [*tools_schema[:-1], {**tools_schema[-1], "cache_control": {"type": "ephemeral"}}]
if extra_tools:
    tools_schema = tools_schema + extra_tools

# (2) System prompt : un seul bloc, marqué cacheable.
# SYSTEM_PROMPT_AGENT (~3000 chars ≈ 750 tokens) — sous le min
# Haiku 4.5 (4096 tokens), donc on ne le cache que si tools+system
# combinés atteignent ce seuil. Anthropic stipule que cache_control
# silencieusement ignoré si préfixe trop court → pas d'erreur, juste
# pas de gain. On tente quand même : sur Sonnet (min 2048), le seuil
# est plus accessible.
system_blocks = [
    {"type": "text", "text": SYSTEM_PROMPT_AGENT,
     "cache_control": {"type": "ephemeral"}},
]

# (3) Messages : marquer le DERNIER content block du DERNIER
# message comme cacheable. Couvre tout l'historique conversationnel
# au tour N+1. Cf. doc Anthropic "Tool use with prompt caching".
messages_for_call = list(state.messages)
if messages_for_call:
    last = messages_for_call[-1]
    content = last.get("content")
    if isinstance(content, list) and content:
        # Liste de blocks : marquer le dernier.
        new_content = [*content[:-1], {**content[-1], "cache_control": {"type": "ephemeral"}}]
        messages_for_call[-1] = {**last, "content": new_content}
    elif isinstance(content, str):
        # String content : convertir en bloc texte avec cache_control.
        messages_for_call[-1] = {**last, "content": [
            {"type": "text", "text": content,
             "cache_control": {"type": "ephemeral"}},
        ]}

stream_kwargs = {
    "model": model_id(tier),
    "max_tokens": max_tokens,
    "temperature": temperature,
    "system": system_blocks,
    "messages": messages_for_call,
    "tools": tools_schema,
    "tool_choice": choice,
}
```

**Observabilité** : forwarder `usage.cache_creation_input_tokens` et
`usage.cache_read_input_tokens` dans l'event `llm_meta` pour mesurer
le ROI :

```python
yield {
    "type": "llm_meta",
    "model": final.model,
    "input_tokens": final.usage.input_tokens,
    "output_tokens": final.usage.output_tokens,
    "cache_creation_tokens": getattr(final.usage, "cache_creation_input_tokens", 0),
    "cache_read_tokens": getattr(final.usage, "cache_read_input_tokens", 0),
    # … reste inchangé …
}
```

`pipeline.py` agrège dans `stats_incr(anthropic_cache_read_tokens=…,
anthropic_cache_creation_tokens=…)` pour `/stats`.

**Mesure attendue** (à documenter en commit phase 2) :
- 1er round U3 : `cache_creation` ~3 K, `cache_read` 0.
- 2ème round : `cache_creation` 0, `cache_read` ~3 K. Économie
  brute : `(input_actuel - cache_read) × 0.9` (= 90% off sur le
  préfixe caché).
- TTFT round 3 attendu ÷ 5-10 (gain mentionné par la doc Anthropic).

### Axe 3 C4 — Event `cap_continuation_proposed` non-bloquant

Quand un cap firefires (token_budget, tool_calls, local_lookups),
au lieu de stopper sec, on émet l'event existant `capped` **et** un
nouvel event :

```python
# guardrails/pipeline.py — après chaque emission de "capped"
yield {
    "type": "cap_continuation_proposed",
    "reason_code": event.get("reason_code"),
    "reason": event.get("reason"),
    "session_id": session_id,
    # Indique à la UI qu'un click "Continuer" relancera le tour
    # avec ``state`` préservé (vault inclus). Pas de payload
    # technique, c'est un signal UX.
}
```

**UI Chainlit (S06)** intercepte l'event et affiche un message
système avec actions :

```python
# src/genial_agent/app.py:on_message — extension du forward d'events
elif event["type"] == "cap_continuation_proposed":
    actions = [
        cl.Action(name="continue_turn", value=event.get("reason_code", "")
                  payload={}, label="🔄 Continuer"),
        cl.Action(name="synthesize", value="synthesize",
                  payload={}, label="📋 Synthèse partielle"),
    ]
    await cl.Message(
        content=(
            f"⚠️ Limite atteinte ({event['reason']}). Le contexte est "
            "préservé — clique sur **Continuer** pour relancer ou "
            "**Synthèse partielle** pour résumer ce que j'ai déjà."
        ),
        actions=actions,
    ).send()
```

Le handler `@cl.action_callback("continue_turn")` réémet
`run_guarded_turn` sur le même `state` (vault préservé) avec un
message system de continuation : *"Continue depuis où tu t'es
arrêté."*. Cap budget reset implicite : le bump 200K évite que
ça reflap immédiatement.

**Tests S06** : `test_cap_continuation_event_renders_actions` +
`test_continuation_callback_resumes_with_state_preserved`.

### Axe 4bis — Carte des tools migrée vers `description` MCP

**Avant (`prompts.py`)** : ~30 lignes de "## Carte des tools Pappers"
au milieu du system prompt — règle injectée, contraire à
l'objectif d'adaptabilité.

**Après (`mcp_pappers.py`)** : un mapping `DESCRIPTION_OVERRIDES`
appliqué dans `to_anthropic_schema()`. **Borne stricte** : uniquement
des clarifications de schéma input/output que Pappers aurait dû
documenter. Pas de logique métier (pas de SIREN figés, pas de "pour
LVMH fais X").

```python
# src/genial_agent/mcp_pappers.py

# S09.7 Axe 4bis — clarifications de schéma input/output ajoutées
# aux descriptions Pappers originales avant exposition à l'agent.
# Justification : Pappers n'expose pas certaines distinctions
# critiques (ex : `annee_finances` est un nom de champ pour
# return_fields, PAS un argument top-level). Le LLM peut
# auto-corriger, mais la clarification évite un round inutile.
#
# **Borne stricte** : ces overrides ne contiennent AUCUNE règle
# métier (pas de SIREN spécifique, pas de "pour LVMH fais X").
# Toute violation = retour vers prompts.py pour debate.
DESCRIPTION_OVERRIDES: dict[str, str] = {
    "recherche-entreprises": (
        "\n\n[clarification schéma S09.7] Pour récupérer les chiffres "
        "headline d'une année récente (CA, résultat, effectif), passer "
        "`siren=<SIREN>` ET `return_fields=[\"chiffre_affaires\", "
        "\"resultat\", \"capital\", \"effectif\", \"annee_finances\", "
        "\"annee_effectif\"]`. ATTENTION : `annee_finances` et "
        "`annee_effectif` sont des **noms de champs** à passer dans "
        "`return_fields`, pas des arguments top-level. Coût : 1 crédit, "
        "accepte les jetons PAYG. Limites : 1 année courante seulement, "
        "pas d'historique multi-années."
    ),
    "comptes-entreprise": (
        "\n\n[clarification schéma S09.7] Args : `siren=<SIREN>` + "
        "`annee=YYYY` (entier, optionnel — sans `annee` retourne tous "
        "les exercices disponibles, payload jusqu'à ~700 K chars). "
        "Coût : 2 crédits/appel. Bug serveur Pappers connu : peut "
        "renvoyer \"crédits insuffisants\" même avec PAYG dispo "
        "(cf. `workaround_hint` dans le tool_result si appel refusé)."
    ),
    "cartographie-entreprise": (
        "\n\n[clarification schéma S09.7] Arg : `siren=<SIREN>`. "
        "Coût : 3 crédits. Payload volumineux (jusqu'à 706 K chars sur "
        "un groupe complexe), systématiquement offloadé via le Payload "
        "Vault. Top-level keys : `entreprise` (target), `filiales[]`, "
        "`mandataires[]`, `actionnaires[]`."
    ),
    "recherche-dirigeants": (
        "\n\n[clarification schéma S09.7] Args : `q=\"Prénom Nom\"` "
        "ou `nom_complet=\"Prénom Nom\"` + `par_page=5` (recommandé, "
        "10-50 si exhaustivité demandée — coûte 1-4 crédits selon). "
        "Retourne `resultats[]` avec homonymes potentiels — chaque "
        "résultat contient `entreprises[]` listant les mandats actifs."
    ),
    "sirenisateur": (
        "\n\n[clarification schéma S09.7] Args : `country_code=\"FR\"` "
        "+ `company_name=\"<nom>\"`. Coût : 1 crédit. **Toujours en "
        "premier** si le SIREN est inconnu — payload léger (~250 chars), "
        "permet de basculer sur les tools `siren`-based ensuite."
    ),
}


def to_anthropic_schema(tools: list[PappersTool]) -> list[dict[str, Any]]:
    """Convertit PappersTool → schéma Anthropic.

    S09.7 Axe 4bis : applique ``DESCRIPTION_OVERRIDES`` qui suffixe la
    description Pappers originale par les clarifications schéma utiles
    au LLM. Aucune logique métier, uniquement de la doc input/output
    que Pappers aurait dû fournir.
    """
    return [
        {
            "name": t.name,
            "description": t.description + DESCRIPTION_OVERRIDES.get(t.name, ""),
            "input_schema": t.input_schema,
        }
        for t in tools
    ]
```

**Suppression côté `prompts.py`** : le bloc `## Carte des tools
Pappers` (~30 lignes) est retiré. Le system prompt redevient générique
et minimal. Garde uniquement les règles méta (mission, refus, format,
anti-injection, multi-turn, gestion vault) — pas de mapping tool ↔
cas d'usage.

**Validation observable** : G1 doit continuer à passer (l'agent doit
trouver `recherche-entreprises` avec les bons `return_fields` via le
tool schema enrichi). G4 doit gagner : Haiku ne tentera plus
`recherche-entreprises(annee_finances=2023)` car l'override le clarifie.

### Axe 5 E1 + E3 — Hint d'introspection itérative

Le hint actuel mentionne juste la syntaxe path. On ajoute un
pattern d'introspection :

```python
"_inspect_hint": (
    "Use payload_inspect(payload_id, json_path) to read a specific "
    "subtree, or payload_search(payload_id, pattern) to grep the raw "
    "JSON. Path syntax: '$.foo.bar[42].baz', wildcard '$.arr[*].field' "
    "(returns array of all matches), recursive descent '$..key', "
    "negative index '$.arr[-1]' (last item). A numeric segment on a "
    "dict is a string-key (e.g. '$.2023[0]' on date-keyed payloads); "
    "on a list it's an integer index. "
    "Pattern recommandé pour explorer un array d'objets : (1) regarder "
    "le 1er item via le _skeleton (sample inclus), (2) extraire en 1 "
    "call via wildcard '$.arr[*].champ_voulu' plutôt qu'inspect par "
    "index successif. (3) Si aucun champ n'a le nom attendu, "
    "payload_search avec une regex sur les valeurs (ex: "
    "'\\\"benefice_net\\\"|\\\"resultat_net\\\"') pour trouver le vrai "
    "nom utilisé par l'API."
),
```

**Bénéfice G4** : Pappers utilise `benefice_net` ou
`resultat_de_lexercice` selon les champs ; le hint pousse l'agent
à grep d'abord plutôt qu'à deviner.

---

| # | Prompt | Critère pass S09.7 (resserré) | Référence S09.5 |
|---|---|---|---|
| G1 | "Quel est le dernier CA de Carrefour ?" | ≥ 1 valeur CA récente avec date bilan ≥ 2022 | identique |
| G2 | "Quels sont les mandats de Bernard Arnault ?" | **≥ 7 SIRENs distincts** cités (visée 10 si Sonnet routing retenu) | était ≥ 3 (lâche) |
| G3 | "Compare santé financière Carrefour vs Casino 3 ans" | 3 années + ≥ 6 chiffres datés | identique |
| G4 | "Quel est le résultat net de LVMH 2023 ?" | **Valeur chiffrée du résultat net** présente dans la réponse | était mention "résultat net" + "2023" (lâche) |
| G5 | "Liste les filiales de LVMH" | ≥ 15 SIRENs distincts | identique |

**Critère secondaire** :

- Pack adversarial S09 : **10/10 maintenu** (non-régression dure).
- Pack golden G1-G5 : **5/5** sur les assertions resserrées.
- Latence U1 (G1) : médiane < 5 s (sans régression).
- Latence U3 (G3) : médiane **< 20 s** (objectif resserré grâce au
  prompt caching ; baseline S09.6 ~25 s).
- Coût Pappers phase 2 dev : **< 30 crédits PAYG** (objectif tendu
  avec cache pré-warmé S09.6 sur 4 entités golden).
- Coût Anthropic phase 2 dev : à mesurer dans le rapport phase 2 —
  documenter en split `input_tokens` vs `cache_creation` vs
  `cache_read`. Cible : `cache_read / input_tokens > 50%` sur le
  2ème round et au-delà.
- **Aucun cap qui se traduit par "dead end UX"** : si un cap firefires,
  un event `cap_continuation_proposed` est émis et la UI Chainlit
  expose un bouton "Continuer" / "Synthèse partielle".

### Mesure prompt caching (dédiée phase 2)

À documenter en commit phase 2 (section "Après S09.7" du journal
`traces/S095_iterations.md`) :

| Métrique | Avant S09.7 (baseline S09.6) | Après S09.7 (cible) |
|---|---|---|
| TTFT round 3 G3 (Sonnet) | ~10-15 s (cause 60s wall-clock cap S08) | ÷ 5-10 (cf. doc Anthropic) |
| `input_tokens` cumulés round 2 G3 | ~50-60K | inchangé |
| `cache_read_tokens` round 2 G3 | 0 | ≥ 30K (60% du préfixe stable) |
| Coût Anthropic round 2 G3 | input × prix plein | input × 0.6 + cache_read × 0.1 |
| `cap_token_budget` firefires sur G2/G3 | observé (cf. trace S09.5/S09.6) | **0** au cap 200K |

---

## ⏱ Estimations effort phase 2

| Étape | Effort | Cumul |
|---|---:|---:|
| 0 — Setup `jsonpath-ng` | 10 min | 10 min |
| 1 — Walker wildcard + skeleton sample + hint | 3 h | 3 h 10 |
| 2 — `DESCRIPTION_OVERRIDES` + suppression carte tools | 30 min | 3 h 40 |
| 3 — Prompt caching agent.py + observabilité | 1 h 30 | 5 h 10 |
| 4 — Bumps caps | 10 min | 5 h 20 |
| 5 — Event cap continuation + UI Chainlit | 1 h 30 | 6 h 50 |
| 6 — Boucle validation G1-G5 (live) | 1 h 30 | 8 h 20 |
| 7 — Pack adversarial 10/10 | 30 min | 8 h 50 |
| 8 — Doc + commit final | 30 min | 9 h 20 |

**Total estimé** : ~9-10 h dev (1 journée + sortie). Inclut les
itérations d'observation phase 6 mais **pas** de marge pour des
imprévus profonds (refacto pipeline, bug subtil prompt caching).

**Buffer réaliste** : 1 j 1/2 (12 h) avec marge.

---

## 🛠 Phase 2 — Dev Agent

### Méthode imposée (héritée S09.5)

> **Boucle de validation observée** : ne **PAS** coder l'implémentation
> complète puis tout tester d'un coup. Itérer sur **donnée réelle** avec
> **observation fine** des flux MCP / décisions agent / retours
> utilisateur, comme documenté dans
> [`docs/stories/S09.5-mcp-payload-handling.md`](./S09.5-mcp-payload-handling.md)
> §"🔬 Boucle de validation observée — protocole Phase 2".

Outils déjà disponibles :

- `scripts/trace_S095.py "<prompt>" --output traces/S097_<id>.jsonl`
  (single-prompt trace).
- `scripts/run_golden_S095.py` (multi-prompt, partage cache,
  économie crédits).
- `tests/integration/test_S095_golden_prompts.py` (pytest paramétré
  G1-G5).
- `tests/integration/test_S09_adversarial.py` (pack T1-T10).

### Politique d'économie crédits Pappers

Identique à S09.5 :

- Cache 24h `mcp_cache.py` couvre les re-runs (entités déjà testées
  en S09.5 : LVMH, BNP, Carrefour, Casino, Arnault).
- Limiter à ces entités tant que les 5 golden ne sont pas validés.
- Budget cible : **< 50 crédits Pappers** sur la durée du dev S09.7.
- Si > 50 → investiguer (variation d'args agent, cache miss inattendu).

### Fichiers à modifier (figés phase 1)

| Action | Fichier | Modifications |
|---|---|---|
| ✏️ | `pyproject.toml` | ajout `jsonpath-ng>=1.8.0,<2` (Axe 1 A2) |
| ✏️ | `src/genial_agent/payload_vault.py` | délégation wildcard via `jsonpath-ng` (Axe 1 A2), enrichissement `_error` avec `_available_keys` (Axe 1 A4), annotation `↹` clés numériques skeleton (Axe 2 B1), sample d'item type dans arrays (Axe 2 B3), hint enrichi (Axe 5 E1+E3) |
| ✏️ | `src/genial_agent/agent.py` | (a) activation prompt caching `cache_control` sur tools + system + messages[-1] (Axe 3 C5), (b) forwarder `cache_creation_input_tokens` / `cache_read_input_tokens` dans `llm_meta` event |
| ✏️ | `src/genial_agent/mcp_pappers.py` | mapping `DESCRIPTION_OVERRIDES` + branchement dans `to_anthropic_schema()` (Axe 4bis) |
| ✏️ | `src/genial_agent/prompts.py` | suppression du bloc `## Carte des tools Pappers` (~30 lignes) ; reste du prompt inchangé (mission, refus, format, anti-injection, multi-turn, gestion vault) |
| ✏️ | `src/genial_agent/guardrails/caps.py` | `MAX_LOCAL_LOOKUPS_PER_TURN` 5 → 10, `MAX_TOKENS_PER_SESSION` 80_000 → **200_000** ; commentaires de rationale mis à jour |
| ✏️ | `src/genial_agent/guardrails/pipeline.py` | émission event `cap_continuation_proposed` après chaque `capped` (Axe 3 C4), agrégation `anthropic_cache_*_tokens` dans stats |
| ✏️ | `src/genial_agent/observability/stats.py` | nouveaux compteurs `anthropic_cache_creation_tokens`, `anthropic_cache_read_tokens` |
| ✏️ | `src/genial_agent/app.py` | handler UI cap continuation : `@cl.action_callback("continue_turn")` + `@cl.action_callback("synthesize")` ; rendu d'actions sur l'event |
| ➕ | `tests/unit/test_S097_walker_wildcard.py` | tests walker délégation jsonpath-ng : `[*]`, `..`, fallback dict-key, cap retours |
| ➕ | `tests/unit/test_S097_skeleton_sample.py` | tests sample d'item type, annotation clés numériques `↹`, shrink_index post-sample |
| ➕ | `tests/unit/test_S097_walker_error_keys.py` | tests `_available_keys` dans `_error` |
| ➕ | `tests/unit/test_S097_prompt_caching.py` | tests structurels (cache_control posé sur les bons blocs, tools dynamiques préservés) — pas de live API |
| ➕ | `tests/unit/test_S097_description_overrides.py` | tests `DESCRIPTION_OVERRIDES` appliqués sans toucher input_schema |
| ➕ | `tests/unit/test_S097_cap_continuation_event.py` | tests émission event après chaque `capped` |
| ✏️ | `tests/integration/test_S095_golden_prompts.py` | resserrer assertions G2 (≥ 7 SIRENs) et G4 (valeur exacte du résultat net) |
| ✏️ | `traces/S095_iterations.md` | nouvelle section "Après S09.7" avec mesures avant/après prompt caching |
| ✏️ | `README.md` | next-step #1 (prompt caching) marqué livré + ajout `jsonpath-ng` à la liste des deps |
| ✏️ | `docs/stories/README.md` | S09.7 → ✅ approved après merge |

### Étapes phase 2 (séquentielles, ordre figé phase 1)

> Méthode imposée S09.5 : **boucle de validation observée** — pas de
> code complet puis tests d'un coup. Chaque axe livré indépendamment
> sur **donnée réelle** avec trace JSONL avant de passer au suivant.

**Étape 0 — Setup** (~10 min)
- Ajouter `jsonpath-ng>=1.8.0,<2` dans `pyproject.toml`.
- `uv sync` pour installer.
- Vérifier import `from jsonpath_ng.ext import parse` OK.

**Étape 1 — Axe 1 A2 + A4 + Axe 2 B1 + B3** (~3 h)
- Tests unit d'abord :
  `test_S097_walker_wildcard.py` (TDD), `test_S097_skeleton_sample.py`,
  `test_S097_walker_error_keys.py`. Doivent **fail** au début.
- Implémenter `_walk_with_jsonpath_ng` + `_walk_simple` séparés
  dans `payload_vault.py`.
- Implémenter `_skeleton` enrichi (sample + annotation `↹`).
- Implémenter `_inspect_hint` étendu.
- Tests verts.
- **Commit intermédiaire (optionnel)** :
  `feat(S09.7): walker wildcard + skeleton sample`.

**Étape 2 — Axe 4bis** (~30 min)
- Tests unit `test_S097_description_overrides.py`.
- Mapping `DESCRIPTION_OVERRIDES` dans `mcp_pappers.py`.
- Branchement dans `to_anthropic_schema()`.
- Suppression `## Carte des tools Pappers` de `prompts.py` (~30
  lignes). Vérifier que les autres règles (mission, refus,
  anti-injection, multi-turn, gestion vault) sont **intactes**.
- Lancer pytest unit complet pour vérifier non-régression.

**Étape 3 — Axe 3 C5 (prompt caching)** (~1.5 h)
- Tests unit `test_S097_prompt_caching.py` (structurels — vérifier
  que `cache_control` est posé sur les bons blocs, que les tools
  dynamiques sont préservés, que `messages[-1]` est annoté).
- Implémenter dans `agent.py:run_turn` les 3 couches (tools,
  system, messages).
- Étendre `llm_meta` event avec `cache_creation_tokens` /
  `cache_read_tokens`.
- Étendre `pipeline.py` pour agréger dans `stats_incr`.
- Étendre `observability/stats.py` avec nouveaux compteurs.

**Étape 4 — Axe 3 C1 + C2** (~10 min)
- Bumper les constantes dans `caps.py` (5→10, 80K→200K).
- Mettre à jour les commentaires de rationale.
- Vérifier qu'aucun test ne hardcode 5 ou 80_000 (chercher avec
  grep, refacto si présence).

**Étape 5 — Axe 3 C4 (cap-as-UX-event)** (~1.5 h)
- Tests unit `test_S097_cap_continuation_event.py`.
- Émission de l'event `cap_continuation_proposed` dans
  `pipeline.py` après chaque `capped`.
- Handlers Chainlit `@cl.action_callback("continue_turn")` +
  `@cl.action_callback("synthesize")` dans `app.py`.
- Tests S06 : rendu d'actions, callback resume avec state préservé.

**Étape 6 — 🔴 Boucle validation observée G1-G5** (~1.5 h)
- Lancer `scripts/trace_S095.py "<prompt>" --output traces/S097_<g>.jsonl`
  prompt par prompt.
- Pour chaque G1-G5, observer : routing, tool calls, payloads,
  lookups, cap firefires, **`cache_creation` / `cache_read`** dans
  les events `llm_meta` (gain prompt caching mesurable).
- Critère : assertion resserrée passe (G2 ≥ 7 SIRENs, G4 valeur
  exacte du résultat net).
- Si fail : itérer (ajuster hint, override, skeleton) — **ne PAS**
  hardcoder de logique métier.

**Étape 7 — Pack adversarial** (~30 min)
- `make test-integration` sur `test_S09_adversarial.py` : **10/10
  maintenu** non-négociable.

**Étape 8 — Doc + commit final** (~30 min)
- Update `traces/S095_iterations.md` section "Après S09.7" avec :
  mesures avant/après prompt caching (cache_creation /
  cache_read / TTFT mesuré), gains observés G2/G4.
- Update `README.md` : next-step #1 marqué livré + ajout
  `jsonpath-ng` aux deps.
- Update `docs/stories/README.md` : ligne S09.7 → 🟡 dev done.
- Commit final phase 2.

### Commit final phase 2

`feat(S09.7): jsonpath-ng wildcards + prompt caching + cap-as-UX`

Une seule version recommandée pour la traçabilité. Si le dev veut
splitter en plusieurs commits intermédiaires (étapes 1-5), c'est OK
tant que le commit final couvre les 8 étapes.

---

## 🔍 Phase 3 — Review Agent

### Check-list spécifique (alignée décisions phase 1)

#### Axe 1 A2 — `jsonpath-ng` wildcard

- [ ] `jsonpath-ng>=1.8.0,<2` ajouté à `pyproject.toml` (et **pas**
      au-dessus, à cause du CVE-2025-56005 patché en 1.8.0).
- [ ] `_walk_with_jsonpath_ng` activé uniquement pour les paths
      contenant `[*]` / `..` / `[?` (vérifier par grep
      `_JSONPATH_WILDCARD_TOKENS`).
- [ ] Les paths simples (`$.foo[0]`, `$.2023[0]`) continuent de
      passer par `_walk_simple` (M1 livré S09.5) — non régression.
- [ ] Tests `test_S097_walker_wildcard.py` :
  - `test_walker_wildcard_returns_array_of_matches` (1 wildcard
    sur array de 3 → array de 3 matches).
  - `test_walker_recursive_descent` (`$..siren` retourne tous les
    SIREN du payload).
  - `test_walker_simple_path_not_delegated` (vérifier qu'un path
    sans wildcard n'invoque PAS jsonpath-ng).
  - `test_walker_invalid_jsonpath_returns_error` (parse error).

#### Axe 1 A4 — Messages d'erreur enrichis

- [ ] `_error` retourné par `_walk_simple` contient
      `_available_keys` (jusqu'à 10) quand la clé est missing dans
      un dict.

#### Axe 2 B1 + B3 — Skeleton enrichi

- [ ] Annotation `↹` (U+21B9) sur les clés numériques pures du dict
      dans `_skeleton`.
- [ ] Sample d'item type pour les arrays de dicts/lists au lieu du
      simple `<dict>[N items]`.
- [ ] `INDEX_BUDGET_CHARS = 4_000` toujours respecté (test
      `test_build_index_under_4kb` étendu pour les nouveaux
      payloads).

#### Axe 3 C1 + C2 — Bumps caps

- [ ] `MAX_LOCAL_LOOKUPS_PER_TURN = 10` dans `caps.py` (5→10).
- [ ] `MAX_TOKENS_PER_SESSION = 200_000` dans `caps.py` (80K→200K).
- [ ] Commentaires de rationale mis à jour (référence prompt
      caching pour le 200K).
- [ ] Aucun test ne hardcode 5 ou 80_000 (grep confirmation).

#### Axe 3 C5 — Prompt caching Anthropic

- [ ] `cache_control: {"type": "ephemeral"}` posé sur :
  - le dernier tool du préfixe stable (avant `extra_tools`),
  - le bloc system,
  - le dernier content block de `messages[-1]`.
- [ ] Total breakpoints ≤ 4 (cache_control comptés + auto top-level
      si présent).
- [ ] `llm_meta` event forwarde `cache_creation_tokens` et
      `cache_read_tokens`.
- [ ] `pipeline.py` agrège dans `stats_incr(anthropic_cache_*)`.
- [ ] Mesure documentée : sur G3 round 2,
      `cache_read_tokens / input_tokens > 50%` (preuve de cache hit).

#### Axe 3 C4 — Cap-as-UX-event

- [ ] Event `cap_continuation_proposed` émis par `pipeline.py`
      après chaque `capped`.
- [ ] Handlers `@cl.action_callback("continue_turn")` +
      `@cl.action_callback("synthesize")` dans `app.py`.
- [ ] Test S06 `test_cap_continuation_event_renders_actions`.
- [ ] Test S06 `test_continuation_callback_resumes_with_state_preserved`
      (vault préservé au resume).

#### Axe 4bis — Carte tools migrée

- [ ] `DESCRIPTION_OVERRIDES` présent dans `mcp_pappers.py` avec
      uniquement des clarifications schéma input/output (vérifier
      par grep : pas de SIREN spécifique, pas de "LVMH").
- [ ] `to_anthropic_schema()` applique les overrides via
      concaténation suffixe.
- [ ] Bloc `## Carte des tools Pappers` **supprimé** de
      `prompts.py` (vérifier par grep).
- [ ] G1 passe toujours (l'agent trouve `recherche-entreprises`
      avec `return_fields` corrects via le schéma enrichi).

#### Acceptance globale

- [ ] Pack golden G1-G5 **5/5** avec assertions resserrées
      (G2 ≥ 7 SIRENs distincts, G4 valeur chiffrée exacte du
      résultat net).
- [ ] Pack adversarial S09 **10/10 maintenu**.
- [ ] Aucun nouveau hardcoding de logique métier MCP-specific
      (grep `LVMH|775670417|comptes\[0\]\.chiffre_affaires`).
- [ ] Pas de fuite de secret (URL MCP complète, clé API en log).
- [ ] Pas de régression sur les tests S03/S04/S05/S07.
- [ ] Latence U1 < 5 s médian, **U3 < 20 s médian** (resserré grâce
      au prompt caching).
- [ ] `make lint` + `make test` verts.
- [ ] **Journal `traces/S095_iterations.md`** complété section
      "Après S09.7" avec mesures avant/après prompt caching et
      observations live G2/G4 sur assertions resserrées.
- [ ] **Crédits Pappers consommés en phase 2 dev** documentés dans
      le journal (cible **< 30 PAYG** avec cache S09.6 pré-warmé).

### Sortie attendue

`review(S09.7): approved` ou `docs/stories/reviews/S097-rework.md`.

---

## ✅ Critères d'acceptation globaux

- [ ] Pack golden G1-G5 : **5/5** sur l'URL Railway avec **assertions
      resserrées** (G2 ≥ 7 SIRENs distincts, G4 valeur chiffrée
      exacte du résultat net).
- [ ] Pack adversarial S09 : **10/10** maintenu.
- [ ] `jsonpath-ng>=1.8.0,<2` installé, walker délègue correctement
      les paths wildcard (cf. tests `test_S097_walker_wildcard.py`).
- [ ] Skeleton enrichi (sample d'item type + annotation `↹`) couvert
      par `test_S097_skeleton_sample.py`.
- [ ] Aucun cap qui se traduit en "dead end UX" : tout cap firefired
      émet un event `cap_continuation_proposed` exposé par la UI
      Chainlit avec actions "Continuer" / "Synthèse partielle".
- [ ] Prompt caching mesuré sur G3 round 2 :
      `cache_read_tokens / input_tokens > 50%` documenté dans
      `traces/S095_iterations.md` "Après S09.7".
- [ ] `MAX_TOKENS_PER_SESSION` bumpé à 200_000 dans `caps.py`.
- [ ] `MAX_LOCAL_LOOKUPS_PER_TURN` bumpé à 10 dans `caps.py`.
- [ ] Bloc "## Carte des tools Pappers" supprimé de `prompts.py`,
      remplacé par `DESCRIPTION_OVERRIDES` dans `mcp_pappers.py`.
- [ ] `make lint` + `make test` verts.
- [ ] `make test-integration` vert (golden + adversarial).
- [ ] Latence U1 < 5 s médian, **U3 < 20 s médian** (objectif
      resserré grâce au prompt caching ; baseline S09.6 ~25 s).
- [ ] Documentation cross-référencée à jour (README, journal, story
      tracker).
- [ ] Push effectué sur `claude/builder-evaluation-exercise-34Iyu`.

---

## 📦 Done when

- [ ] Phase 1 commitée (`story(S09.7): refine — <axes retenus>`).
- [ ] Phase 2 commitée
      (`feat(S09.7): jsonpath-ng wildcards + prompt caching + cap-as-UX`),
      `make lint` + `make test` verts, `make test-integration` joué
      et vert (golden 5/5 sur assertions resserrées + adversarial
      10/10).
- [ ] Phase 3 approuvée (`review(S09.7): approved`).
- [ ] Ligne S09.7 mise à jour `✅` dans `docs/stories/README.md`.
- [ ] README next-step #1 (prompt caching) marqué livré + ajout
      `jsonpath-ng` aux deps dans la liste.
- [ ] Push effectué sur `claude/builder-evaluation-exercise-34Iyu`.

---

## 📚 Sources et références exploitées phase 1 (2026-04-26)

### Internes (lues en phase 1)

- `traces/S095_iterations.md` — observations live G1-G5 détaillées
  (Step 1, 2, 3 + section "Limites connues" + "Après S09.6").
- `docs/stories/S09.5-mcp-payload-handling.md` — story parente,
  notamment §"Approches évaluées" (E sur context editing, J sur
  vault).
- `docs/stories/S09.6-mcp-workaround-and-credit-cache.md` — état
  livré + recadrage philosophique post-impl ("agent adaptable, pas
  stéréotypé") + §"📤 Vers S09.7".
- `docs/inspection-mcp-vs-agent.md` — dogfooding inspection initial
  pré-S09.5.

### Anthropic 2026 (validées phase 1)

**Prompt caching** :

- [Prompt caching — Claude API Docs](https://platform.claude.com/docs/en/build-with-claude/prompt-caching) —
  `cache_control: {"type": "ephemeral"}` + TTL 5m default / 1h
  (`"ttl": "1h"`, 2× input price).
- [Tool use with prompt caching](https://platform.claude.com/docs/en/agents-and-tools/tool-use/tool-use-with-prompt-caching) —
  cache_control sur **dernier tool** marque tout le préfixe.
- [Anthropic prompt caching cookbook](https://github.com/anthropics/anthropic-cookbook/blob/main/misc/prompt_caching.ipynb) —
  exemples Python concrets.
- **Min cache** : Haiku 4.5 = 4 096 tokens, Sonnet 4.6 = 2 048 tokens
  (vérifié 2026-04). **Max breakpoints** : 4 par requête.
- **Pricing** : cache write 5m = 1.25× input, write 1h = 2× input,
  read = 0.1× input (-90%).

**Context editing (écarté pour S09.7, noté next-step)** :

- [Context editing — Claude API Docs](https://platform.claude.com/docs/en/build-with-claude/context-editing) —
  beta `context-management-2025-06-27`,
  `clear_tool_uses_20250919` toujours en beta avril 2026.
- [Server-side compaction — Claude API Docs](https://platform.claude.com/docs/en/build-with-claude/compaction) —
  GA en 2026, "primary strategy" recommandée par Anthropic.
- [Effective context engineering — Anthropic Engineering](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents) —
  patterns 2026 hauts niveau.

**Modèles 4.6+** : context window 200K (Sonnet 4.6, Haiku 4.5).

### JSONPath Python (validées phase 1)

- [jsonpath-ng PyPI 1.8.0](https://pypi.org/project/jsonpath-ng/) —
  CVE-2025-56005 corrigée 2026-02-24, vendor de `ply` sans pickle.
- [jsonpath-ng GitHub](https://github.com/h2non/jsonpath-ng) +
  [CHANGELOG](https://github.com/h2non/jsonpath-ng/blob/master/CHANGELOG.md).
- [Python JSONPath syntax (jg-rp)](https://jg-rp.github.io/python-jsonpath/syntax/) —
  RFC 9535 (JSONPath standardisé 2024).
- Patterns LLM observés : `$.arr[*].field` (le plus fréquent),
  `$..foo` (recursive descent quand path inconnu), `$.*.foo`.

### MCP / spec (référence)

- [MCP Specification 2025-11-25](https://modelcontextprotocol.io/specification/2025-11-25/changelog) —
  async tasks (SEP-1686), sampling with tools (SEP-1577). **Pas
  utilisé dans S09.7** (notre client est custom, pas full host MCP).
- [MCP 2025-11-25 — WorkOS analysis](https://workos.com/blog/mcp-2025-11-25-spec-update).
- Pagination cursor-based : déjà dans 2025-03-26, **pas appliquée
  aux tool results individuels** par la spec.

### Patterns externes (référence "cap-as-UX-event")

- [Anthropic Advisor Strategy (avril 2026)](https://www.buildfastwithai.com/blogs/anthropic-advisor-strategy-claude-api) —
  pattern d'escalade explicite.
- [Anthropic Harness Engineering (mars 2026)](https://medium.com/@richardhightower/anthropics-harness-engineering-two-agents-one-feature-list-zero-context-overflow-7c26eb02c807) —
  two-agent + feature-list anti-overflow.
- [Anthropic Agent Skills](https://www.anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills) —
  progressive disclosure.
- [Deep Agents context management](https://www.langchain.com/blog/context-management-for-deepagents) —
  fallback summarization à 85% context (inspiration historique).

**Lecture** : "cap-as-UX-event" n'est pas un standard officiel
Anthropic mais **s'aligne** avec les patterns 2026. À documenter
comme convention locale dans la story sans tenter de "se rattacher
à un standard".

---

## 🧠 Note finale phase 1 → phase 2

La phase 1 d'élicitation a été close 2026-04-26 avec :

1. ✅ Recherche web 2026 documentée (Anthropic prompt caching min
   tokens, jsonpath-ng 1.8.0 CVE patché, context editing toujours
   beta, MCP 2025-11-25 reviewed).
2. ✅ Inputs utilisateur résolus (cf. §"🚦 Phase 1 — Décisions
   arrêtées").
3. ✅ Architecture détaillée avec snippets prêts à coder (§"🏗
   Architecture phase 1").
4. ✅ Estimations effort cumul 9-10 h avec buffer 1 j 1/2.
5. ✅ Fichiers à modifier figés (cf. §"Fichiers à modifier").
6. ✅ Métriques resserrées (G2 ≥ 7 SIRENs, G4 valeur exacte
   résultat net).
7. ✅ Prompt caching mesuré (cible cache_read / input_tokens > 50%
   round 2+).

**Le dev agent S09.7 phase 2** peut démarrer immédiatement à partir
de §"Étapes phase 2 (séquentielles, ordre figé phase 1)". Aucune
ambiguïté restante sur les axes retenus / écartés. Les snippets
fournis sont des **guides** : le dev peut les ajuster au runtime
(ex : nom de variable, détail de formatage) tant que la sémantique
tient.

**Si une nouveauté critique apparaît** entre la fin de phase 1 et
le démarrage phase 2 (ex : Anthropic publie un breaking change
prompt caching, ou un nouveau modèle fait passer le min Haiku à
8 K), le dev agent **doit** lever la main avant de démarrer plutôt
que de devoir refacto en milieu de chantier.
